<p id="notice"><%= notice %></p>

<% provide(:title, @user.name) %>

<!-- Used for sidebar on user pages -->

<div class="row">
	<aside class="col-md-4" > 
		<section class="user_info">
			<h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
		</section>
	</aside>
</div>
<%= @user.email %>
<br>
<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
